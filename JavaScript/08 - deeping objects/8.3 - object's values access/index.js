const item = {
    tipo: "Teclado",
    valor: 'R$190',
    estoque: true
}

console.log(item.tipo);
console.log("O produto " + `${item.tipo}` + " está saindo por " + `${item.valor}`);
console.log(`O produto ${item.tipo} está saindo por ${item.valor}`);