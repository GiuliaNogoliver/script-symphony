let arr = ['a', 1, 2, 3, 4, 5, 'b'];

console.table(arr);

// O shift remove o primeiro element e retorna ele
console.table(arr.shift(arr));
console.table(arr);

// O pop remove o ultimo elemento de um array e retorna ele
console.table(arr.pop(arr));
console.table(arr);